# OpenRoboBrain MuJoCo G1 仿真 + 语音对话依赖
# 安装: pip install -r requirements-sim.txt

# 仿真
mujoco>=3.1.6
torch>=2.0
websockets>=12.0
pyyaml>=6.0
numpy>=1.24

# 语音 (ASR + TTS)
faster-whisper>=1.0
sounddevice>=0.4
edge-tts>=6.0
